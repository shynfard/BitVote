package main

import (
	"os/user"

	"filippo.io/edwards25519"
)

// user authentication
// - if private key is not provided, generate a new one
func authenticate(privateKey *edwards25519.Scalar) {
	// create instnce of user
	user := user.User{
}

// poll creation

// poll voting
